<UserControl x:Class="MasaHotelPro.Views.GuestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F8FAFC">
    
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowHeight" Value="50"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        
        <!-- Main List Area -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,20,0" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Guests Management" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Manage your hotel guests and their profiles." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="40" Padding="10,0" Width="250" Margin="0,0,15,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" Foreground="#94A3B8" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Search Guests..." BorderThickness="0" Background="Transparent" Margin="8,0,0,0" VerticalAlignment="Center" Width="200"/>
                        </StackPanel>
                    </Border>
                    
                    <Button Grid.Column="2" Command="{Binding AddGuestCommand}" Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountPlus" Width="18" Height="18" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Add Guest" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
                
                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Guests}" SelectedItem="{Binding SelectedGuest}" BorderThickness="1" BorderBrush="#E2E8F0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding Name}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                        <DataGridTextColumn Header="Nationality" Binding="{Binding Nationality}" Width="100"/>
                        <DataGridTextColumn Header="ID/Passport" Binding="{Binding DocumentNumber}" Width="150"/>
                        <DataGridTemplateColumn Header="Actions" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Background="Transparent" BorderThickness="0" Command="{Binding DataContext.EditGuestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Cursor="Hand">
                                            <materialDesign:PackIcon Kind="Pencil" Foreground="#3B82F6"/>
                                        </Button>
                                        <Button Background="Transparent" BorderThickness="0" Command="{Binding DataContext.DeleteGuestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="10,0,0,0" Cursor="Hand" ToolTip="Delete">
                                            <materialDesign:PackIcon Kind="DeleteOutline" Foreground="#EF4444" Width="20" Height="20" VerticalAlignment="Center"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        
        <!-- Form Area (Visible only when editing/adding) -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="25" Visibility="{Binding IsFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Guest Profile" FontSize="18" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,20"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Full Name -->
                        <TextBlock Text="Full Name" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Name}" materialDesign:HintAssist.Hint="e.g. John Doe" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Phone -->
                        <TextBlock Text="Phone Number" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Phone}" materialDesign:HintAssist.Hint="e.g. +1 234 567 890" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Email -->
                        <TextBlock Text="Email Address" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Email}" materialDesign:HintAssist.Hint="e.g. john@example.com" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Nationality -->
                        <TextBlock Text="Nationality" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Nationality}" materialDesign:HintAssist.Hint="e.g. American" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Document Number -->
                        <TextBlock Text="ID / Passport Number" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.DocumentNumber}" materialDesign:HintAssist.Hint="e.g. A1234567" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Address -->
                        <TextBlock Text="Home Address" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Address}" materialDesign:HintAssist.Hint="Full address" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Notes -->
                        <TextBlock Text="Special Notes" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GuestForm.Notes}" materialDesign:HintAssist.Hint="Any special requests or notes" TextWrapping="Wrap" MinHeight="60" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Action Buttons -->
                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Command="{Binding CancelCommand}" Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="1" Foreground="#475569" Cursor="Hand" Padding="0,10">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Cancel" FontWeight="SemiBold"/>
                    </Button>
                    
                    <Button Grid.Column="2" Command="{Binding SaveGuestCommand}" Style="{StaticResource PrimaryButton}" Padding="0,10">
                        <TextBlock Text="Save Guest"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
