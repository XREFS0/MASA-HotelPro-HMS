<UserControl x:Class="MasaHotelPro.Views.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F8FAFC">
    
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowHeight" Value="50"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        
        <!-- Main List Area -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,20,0" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Rooms Management" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Manage your hotel rooms and availability." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="40" Padding="10,0" Width="250" Margin="0,0,15,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" Foreground="#94A3B8" VerticalAlignment="Center"/>
                            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Search Rooms..." BorderThickness="0" Background="Transparent" Margin="8,0,0,0" VerticalAlignment="Center" Width="200"/>
                        </StackPanel>
                    </Border>
                    
                    <Button Grid.Column="2" Command="{Binding AddRoomCommand}" Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Add Room" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
                
                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}" BorderThickness="1" BorderBrush="#E2E8F0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="150" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" Width="120"/>
                        <DataGridTemplateColumn Header="Status" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="10,4" HorizontalAlignment="Left" Background="#DCFCE7">
                                        <TextBlock Text="{Binding Status}" Foreground="#15803D" FontSize="12" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Background="Transparent" BorderThickness="0" Command="{Binding DataContext.EditRoomCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Cursor="Hand" Margin="0,0,5,0">
                                            <materialDesign:PackIcon Kind="Pencil" Foreground="#3B82F6"/>
                                        </Button>
                                        <Button Background="Transparent" BorderThickness="0" Command="{Binding DataContext.DeleteRoomCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Cursor="Hand">
                                            <materialDesign:PackIcon Kind="DeleteOutline" Foreground="#EF4444"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
        
        <!-- Form Area (Visible only when editing/adding) -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="25" Visibility="{Binding IsFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Room Details" FontSize="18" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,20"/>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Room Number -->
                        <TextBlock Text="Room Number" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding RoomForm.RoomNumber}" materialDesign:HintAssist.Hint="e.g. 101" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Room Name -->
                        <TextBlock Text="Room Name" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding RoomForm.Name}" materialDesign:HintAssist.Hint="e.g. Ocean View" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                        
                        <!-- Type -->
                        <TextBlock Text="Room Type" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <ComboBox SelectedValue="{Binding RoomForm.Type}" SelectedValuePath="Content" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0">
                            <ComboBoxItem Content="Single Room"/>
                            <ComboBoxItem Content="Double Room"/>
                            <ComboBoxItem Content="Triple Room"/>
                            <ComboBoxItem Content="Suite"/>
                            <ComboBoxItem Content="Deluxe Suite"/>
                            <ComboBoxItem Content="Presidential Suite"/>
                        </ComboBox>
                        
                        <!-- Status -->
                        <TextBlock Text="Status" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <ComboBox SelectedValue="{Binding RoomForm.Status}" SelectedValuePath="Content" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0">
                            <ComboBoxItem Content="Available"/>
                            <ComboBoxItem Content="Occupied"/>
                            <ComboBoxItem Content="Reserved"/>
                            <ComboBoxItem Content="Cleaning"/>
                            <ComboBoxItem Content="Maintenance"/>
                        </ComboBox>

                        <!-- Price -->
                        <TextBlock Text="Price Per Night ($)" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding RoomForm.Price}" materialDesign:HintAssist.Hint="0.00" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,15" Background="#F8FAFC" BorderBrush="#E2E8F0"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Action Buttons -->
                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Command="{Binding CancelCommand}" Background="Transparent" BorderBrush="#E2E8F0" BorderThickness="1" Foreground="#475569" Cursor="Hand" Padding="0,10">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Cancel" FontWeight="SemiBold"/>
                    </Button>
                    
                    <Button Grid.Column="2" Command="{Binding DeleteRoomCommand}" CommandParameter="{Binding RoomForm}" Background="#EF4444" Foreground="White" BorderThickness="0" Cursor="Hand" Padding="0,10">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Delete"/>
                    </Button>
                    
                    <Button Grid.Column="4" Command="{Binding SaveRoomCommand}" Style="{StaticResource PrimaryButton}" Padding="0,10">
                        <TextBlock Text="Save Changes"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
