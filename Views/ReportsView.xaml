<UserControl x:Class="MasaHotelPro.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F8FAFC">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="System Reports" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Analytics, financials, and occupancy reports." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,10" Padding="25">
                <StackPanel>
                    <materialDesign:PackIcon Kind="ChartBar" Width="40" Height="40" Foreground="#3B82F6" Margin="0,0,0,15"/>
                    <TextBlock Text="Financial Report" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Generate detailed reports of all revenues, invoices, and payments over a specific period." TextWrapping="Wrap" Foreground="#64748B" Margin="0,10,0,20"/>
                    <Button Content="Generate Report" Command="{Binding GenerateFinancialReportCommand}" Height="40" Padding="16,0" Background="#EFF6FF" Foreground="#2563EB" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,10" Padding="25">
                <StackPanel>
                    <materialDesign:PackIcon Kind="ChartPie" Width="40" Height="40" Foreground="#10B981" Margin="0,0,0,15"/>
                    <TextBlock Text="Occupancy Report" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Analyze room occupancy rates, average daily rates (ADR), and revenue per available room (RevPAR)." TextWrapping="Wrap" Foreground="#64748B" Margin="0,10,0,20"/>
                    <Button Content="Generate Report" Command="{Binding GenerateOccupancyReportCommand}" Height="40" Padding="16,0" Background="#DCFCE7" Foreground="#10B981" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,10,10,0" Padding="25">
                <StackPanel>
                    <materialDesign:PackIcon Kind="AccountGroup" Width="40" Height="40" Foreground="#8B5CF6" Margin="0,0,0,15"/>
                    <TextBlock Text="Guest Demographics" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Understand your guests better with reports on nationality, booking frequency, and preferences." TextWrapping="Wrap" Foreground="#64748B" Margin="0,10,0,20"/>
                    <Button Content="Generate Report" Command="{Binding GenerateGuestDemographicsCommand}" Height="40" Padding="16,0" Background="#F3E8FF" Foreground="#8B5CF6" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,10,0,0" Padding="25">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Broom" Width="40" Height="40" Foreground="#F59E0B" Margin="0,0,0,15"/>
                    <TextBlock Text="Housekeeping &amp; Maintenance" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Track the performance of housekeeping staff and status of maintenance tasks." TextWrapping="Wrap" Foreground="#64748B" Margin="0,10,0,20"/>
                    <Button Content="Generate Report" Command="{Binding GenerateHousekeepingReportCommand}" Height="40" Padding="16,0" Background="#FEF3C7" Foreground="#D97706" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
