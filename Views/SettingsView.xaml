<UserControl x:Class="MasaHotelPro.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F8FAFC">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="System Settings" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Configure your hotel preferences and application settings." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>
        
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="30">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="General Preferences" FontSize="18" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,20"/>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Hotel Name" FontSize="15" FontWeight="SemiBold" Foreground="#475569" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding HotelName, UpdateSourceTrigger=PropertyChanged}" Height="45" VerticalContentAlignment="Center" Padding="15,0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Background="#F8FAFC" BorderBrush="#CBD5E1"/>
                    </Grid>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Currency" FontSize="15" FontWeight="SemiBold" Foreground="#475569" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" Text="{Binding Currency}" Height="45" VerticalContentAlignment="Center" Padding="15,0" Style="{StaticResource MaterialDesignOutlinedComboBox}" Background="#F8FAFC" BorderBrush="#CBD5E1">
                            <ComboBoxItem Content="USD ($)"/>
                            <ComboBoxItem Content="EUR (€)"/>
                            <ComboBoxItem Content="EGP (E£)"/>
                        </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Tax Rate (%)" FontSize="15" FontWeight="SemiBold" Foreground="#475569" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding TaxRate, UpdateSourceTrigger=PropertyChanged}" Height="45" VerticalContentAlignment="Center" Padding="15,0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Background="#F8FAFC" BorderBrush="#CBD5E1"/>
                    </Grid>



                    <Button Command="{Binding SaveSettingsCommand}" Content="Save Settings" Height="45" Width="160" Background="#2563EB" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" HorizontalAlignment="Left" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
