<UserControl x:Class="MasaHotelPro.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F8FAFC">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30,0,30,30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Time Filter -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Time Period:" Foreground="#64748B" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding TimeFilters}" SelectedItem="{Binding TimeFilter}" Width="150" materialDesign:TextFieldAssist.HasClearButton="False" BorderBrush="#E2E8F0"/>
                </StackPanel>
            </Grid>

            <!-- Top Stats Cards -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Total Bookings -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="56" Height="56" CornerRadius="16" Background="#3B82F6">
                            <materialDesign:PackIcon Kind="Bed" Width="28" Height="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Total Bookings" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding TotalBookings}" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                                <Border CornerRadius="6" Padding="6,2" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsTotalBookingsTrendPositive}" Value="True"><Setter Property="Background" Value="#DCFCE7"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,2,0">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Setter Property="Kind" Value="ArrowDown"/>
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsTotalBookingsTrendPositive}" Value="True"><Setter Property="Kind" Value="ArrowUp"/><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding TotalBookingsTrend}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsTotalBookingsTrendPositive}" Value="True"><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="vs previous period" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Check-ins Today -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,10,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="56" Height="56" CornerRadius="16" Background="#10B981">
                            <materialDesign:PackIcon Kind="Key" Width="28" Height="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Check-ins Today" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding CheckInsToday}" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                                <Border CornerRadius="6" Padding="6,2" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCheckInsTrendPositive}" Value="True"><Setter Property="Background" Value="#DCFCE7"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,2,0">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Setter Property="Kind" Value="ArrowDown"/>
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCheckInsTrendPositive}" Value="True"><Setter Property="Kind" Value="ArrowUp"/><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding CheckInsTrend}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCheckInsTrendPositive}" Value="True"><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="vs previous period" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Guests In House -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10,0,10,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="56" Height="56" CornerRadius="16" Background="#8B5CF6">
                            <materialDesign:PackIcon Kind="AccountMultiple" Width="28" Height="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Guests In House" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding GuestsInHouse}" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                                <Border CornerRadius="6" Padding="6,2" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsGuestsInHouseTrendPositive}" Value="True"><Setter Property="Background" Value="#DCFCE7"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,2,0">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Setter Property="Kind" Value="ArrowDown"/>
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsGuestsInHouseTrendPositive}" Value="True"><Setter Property="Kind" Value="ArrowUp"/><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding GuestsInHouseTrend}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsGuestsInHouseTrendPositive}" Value="True"><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="vs previous period" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Revenue Today -->
                <Border Grid.Column="3" Style="{StaticResource CardStyle}" Margin="10,0,0,0" Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="56" Height="56" CornerRadius="16" Background="#F59E0B">
                            <materialDesign:PackIcon Kind="CurrencyUsd" Width="28" Height="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Revenue" FontSize="13" FontWeight="SemiBold" Foreground="#64748B"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding RevenueToday, StringFormat={}{0:C}}" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                                <Border CornerRadius="6" Padding="6,2" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRevenueTrendPositive}" Value="True"><Setter Property="Background" Value="#DCFCE7"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,2,0">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Setter Property="Kind" Value="ArrowDown"/>
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRevenueTrendPositive}" Value="True"><Setter Property="Kind" Value="ArrowUp"/><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding RevenueTrend}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRevenueTrendPositive}" Value="True"><Setter Property="Foreground" Value="#15803D"/></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="vs previous period" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Middle Charts Area -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Occupancy Overview Chart -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="20" Height="300">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <TextBlock Text="Occupancy Overview" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                        </Grid>
                        <lvc:CartesianChart Grid.Row="1" Margin="0,20,0,0" Series="{Binding OccupancySeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" />
                    </Grid>
                </Border>
                
                <!-- Room Status -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <TextBlock Text="Room Status" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                            <Button Command="{Binding NavigateCommand}" CommandParameter="Rooms" Cursor="Hand" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock Text="View All" Foreground="#2563EB" FontSize="13" FontWeight="SemiBold"/>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Real Pie Chart -->
                            <Grid Grid.Column="0" Width="140" Height="140" Margin="0,0,25,0">
                                <lvc:PieChart Series="{Binding RoomStatusSeries}" InitialRotation="-90" MaxAngle="360" TooltipPosition="Hidden" LegendPosition="Hidden"/>
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TotalRooms}" FontSize="24" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Rooms" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Legend -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                                <Grid Margin="0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="#3B82F6" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="Occupied" Margin="10,0,0,0" FontSize="13" FontWeight="Medium" Foreground="#64748B" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" FontSize="13" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center">
                                        <Run Text="{Binding OccupiedRooms}"/> <Run Text="{Binding OccupiedPercentage}" Foreground="#94A3B8" FontSize="11"/>
                                    </TextBlock>
                                </Grid>
                                <Grid Margin="0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="#10B981" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="Available" Margin="10,0,0,0" FontSize="13" FontWeight="Medium" Foreground="#64748B" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" FontSize="13" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center">
                                        <Run Text="{Binding AvailableRooms}"/> <Run Text="{Binding AvailablePercentage}" Foreground="#94A3B8" FontSize="11"/>
                                    </TextBlock>
                                </Grid>
                                <Grid Margin="0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="#F59E0B" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="Maintenance" Margin="10,0,0,0" FontSize="13" FontWeight="Medium" Foreground="#64748B" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" FontSize="13" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center">
                                        <Run Text="{Binding MaintenanceRooms}"/> <Run Text="{Binding MaintenancePercentage}" Foreground="#94A3B8" FontSize="11"/>
                                    </TextBlock>
                                </Grid>
                                <Grid Margin="0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="#EF4444" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="Out of Srv" Margin="10,0,0,0" FontSize="13" FontWeight="Medium" Foreground="#64748B" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" FontSize="13" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center">
                                        <Run Text="{Binding OutOfServiceRooms}"/> <Run Text="{Binding OutOfServicePercentage}" Foreground="#94A3B8" FontSize="11"/>
                                    </TextBlock>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Bottom Lists Area -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Recent Reservations -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="Recent Reservations" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                            <Button Command="{Binding NavigateCommand}" CommandParameter="Reservations" Cursor="Hand" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock Text="View All" Foreground="#2563EB" FontSize="13" FontWeight="SemiBold"/>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                        
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding RecentReservations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <Grid Margin="0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="40" Height="40" CornerRadius="20" Background="#EFF6FF">
                                                <TextBlock Text="{Binding Guest.Name[0]}" Foreground="#2563EB" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Guest.Name}" FontWeight="Bold" Foreground="#0F172A"/>
                                                <TextBlock FontSize="12" Foreground="#64748B">
                                                    <Run Text="{Binding CheckInDate, StringFormat={}{0:MMM dd}}"/> - <Run Text="{Binding CheckOutDate, StringFormat={}{0:MMM dd, yyyy}}"/>  •  <Run Text="{Binding Room.Type}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="#DCFCE7" CornerRadius="4" Padding="10,5" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Status}" Foreground="#15803D" FontSize="12" FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>
                                        <Separator Background="#F1F5F9" Margin="0,5"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
                
                <!-- Today's Schedule -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="Today's Schedule" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                            <Button Command="{Binding NavigateCommand}" CommandParameter="Reservations" Cursor="Hand" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock Text="View Calendar" Foreground="#2563EB" FontSize="13" FontWeight="SemiBold"/>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                        
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding TodaySchedule}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="65"/>
                                            <ColumnDefinition Width="15"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Time}" FontWeight="SemiBold" Foreground="#0F172A" VerticalAlignment="Center"/>
                                        <Ellipse Grid.Column="1" Width="8" Height="8" Fill="{Binding IconColor}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        <Border Grid.Column="2" Background="#F8FAFC" CornerRadius="8" Padding="12" Margin="10,0,0,0">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="{Binding Icon}" Foreground="{Binding IconColor}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#0F172A"/>
                                                    <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="#64748B"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
