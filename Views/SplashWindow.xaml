<Window x:Class="MasaHotelPro.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Starting MASA HotelPro" Height="350" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    
    <Border CornerRadius="20" Background="#0F172A" BorderBrush="#3B82F6" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Black" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="80" Height="80" CornerRadius="40" Background="#2563EB" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="4" Color="#2563EB" Opacity="0.4"/>
                    </Border.Effect>
                    <materialDesign:PackIcon Kind="DiamondStone" Width="40" Height="40" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="MASA HotelPro" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Premium Hotel Management System" FontSize="14" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                
                <TextBlock Text="Initializing services..." FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,40,0,0" x:Name="StatusText"/>
            </StackPanel>
            
            <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4" BorderThickness="0" Background="Transparent" Foreground="#3B82F6" Margin="40,0,40,30">
                <ProgressBar.Clip>
                    <RectangleGeometry RadiusX="2" RadiusY="2" Rect="0,0,520,4"/>
                </ProgressBar.Clip>
            </ProgressBar>
        </Grid>
    </Border>
</Window>
